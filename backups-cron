# For testing - run every minute
#*/1 * * * * root /backups.sh 2>&1

# Run the backups at 21hrs gmt each day
0 21 * * * root /backups.sh 2>&1

# We need a blank line here for it to be a valid cron file
